<!-- form to add a cocktail -->
<%= simple_form_for(new_cocktail) do |f| %>
      <%= f.input :name %>
      <%= f.input :photo %>
      <%= f.input :photo_cache, as: :hidden %>
      <%= f.submit 'Done !', class: "button-done btn btn-primary" %>
    <% end %>